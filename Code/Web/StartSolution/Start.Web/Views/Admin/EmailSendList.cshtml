@using Webdiyer.WebControls.Mvc;
@model PagedList<Start.Entity.EmailSendInfo>

<p class="highlight">邮箱发送列表 </p>
<form action="/Admin/EmailSendList" class="form-inline" method="get">
     <div class="form-group input-group-sm">
        <label class="control-label">用户编号</label>
        <input type="text" value="@(ViewBag.EmailSendInfo.UserID==0?"":ViewBag.EmailSendInfo.UserID.ToString())" name="userID" class="form-control" />
     </div>
    <div class="form-group">
         <input type="submit" class="btn btn-primary" value="搜索" />
    </div>
</form>
<table class="table table-bordered mt table-hover">
    <thead>
        <tr>
            <th>编号</th>
            <th>用户编号</th>
            <th>邮件名称</th>
            <th>邮件内容</th>
            <th>接受人邮件</th>
            <th>发送日期</th>
        </tr>
    </thead>
    <tbody>
        @{int index = -1; }
        @foreach (var item in Model)
        {
            index += 1;
            <tr>
                    <td>@item.ID</td>
                    <td>@item.UserID</td>
                    <td>@item.Name</td>
                    <td>@item.Memo</td>  
                    <td>@item.ReceiveEmail</td>  
                    <td>@item.Date</td>             
            </tr>
        }
    </tbody>
</table> 
@Html.Pager(Model,new PagerOptions{PageIndexParameterName = "pageIndex",CssClass="pagination"})
