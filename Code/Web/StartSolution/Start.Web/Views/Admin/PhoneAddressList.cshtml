@using Webdiyer.WebControls.Mvc;
@model PagedList<Start.Entity.PhoneAddressInfo>

<p class="highlight">手机联系人列表 </p>
<form action="/Admin/PhoneAddressList" class="form-inline" method="get">
     <div class="form-group input-group-sm">
        <label class="control-label">用户编号</label>
        <input type="text" value="@(ViewBag.PhoneAddressInfo.UserID==0?"":ViewBag.PhoneAddressInfo.UserID.ToString())" name="userID" class="form-control" />
     </div>
    <div class="form-group">
         <input type="submit" class="btn btn-primary" value="搜索" />
    </div>
</form>
<table class="table table-bordered mt table-hover">
    <thead>
        <tr>
            <th>编号</th>
            <th>用户编号</th>
            <th>联系人名</th>
            <th>联系人手机</th>
        </tr>
    </thead>
    <tbody>
        @{int index = -1; }
        @foreach (var item in Model)
        {
            index += 1;
            <tr>
                    <td><a href="/Admin/PhoneAddress/@item.ID">@item.ID</a></td>
                    <td>@item.UserID</td>
                    <td>@item.Name</td>
                    <td>@item.Phone</td>             
            </tr>
        }
    </tbody>
</table> 
@Html.Pager(Model,new PagerOptions{PageIndexParameterName = "pageIndex",CssClass="pagination"})
