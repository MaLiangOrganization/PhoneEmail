@using Webdiyer.WebControls.Mvc;
@model PagedList<Start.Entity.MessageSendInfo>

<p class="highlight">短信发送列表 </p>
<form action="/Admin/MessageSendList" class="form-inline" method="get">
     <div class="form-group input-group-sm">
        <label class="control-label">用户编号</label>
        <input type="text" value="@(ViewBag.MessageSendInfo.UserID==0?"":ViewBag.MessageSendInfo.UserID.ToString())" name="userID" class="form-control" />
     </div>
    <div class="form-group">
         <input type="submit" class="btn btn-primary" value="搜索" />
    </div>
</form>
<table class="table table-bordered mt table-hover">
    <thead>
        <tr>
            <th>编号</th>
            <th>用户编号</th>
            <th>短信内容</th>
            <th>接受人手机</th>
        </tr>
    </thead>
    <tbody>
        @{int index = -1; }
        @foreach (var item in Model)
        {
            index += 1;
            <tr>
                    <td>@item.ID</td>
                    <td>@item.UserID</td>
                    <td>@item.Memo</td>
                    <td>@item.ReceivePhone</td>             
            </tr>
        }
    </tbody>
</table> 
@Html.Pager(Model,new PagerOptions{PageIndexParameterName = "pageIndex",CssClass="pagination"})
